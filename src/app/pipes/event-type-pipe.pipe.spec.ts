import { EventTypePipePipe } from './event-type-pipe.pipe';

describe('EventTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new EventTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
